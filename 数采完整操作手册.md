# 单台 PiPER + D435 + PICO + EVA 完整数采操作手册

更新时间：2026-09-17。适用于本机已经配置的 PiperXR、EVA-CLIENT 和 realsense_src。

本手册覆盖从环境检查到一段数据录制、保存、质检、深度读取、点云导出和备份恢复的完整流程。硬件为一台 PiPER 六轴机械臂、一台 RealSense D435，以及运行 XRoboToolkit 的 PICO 头显和手柄。D435 使用独立 Python 环境，整个集成不依赖 ROS1/ROS2。

**当前状态：软件离线录制、保存、取消、深度关联和点云导出已有模拟验证；真实 D435、CAN、PICO 联合采集和长时间录制仍需要按本文做实机验收。** 具体记录见第 13 节。

## 目录

1. [系统组成与本机路径](#s1)
2. [日常操作速查](#s2)
3. [首次使用：环境配置、依赖与软件检查](#s3)
4. [先完成一次纯模拟录制](#s4)
5. [真机准备：D435、CAN、PICO](#s5)
6. [逐步完成一段真机数据采集](#s6)
7. [数据目录、字段、单位和时间戳](#s7)
8. [检查一段数据是否完整](#s8)
9. [深度图与点云导出](#s9)
10. [多任务、会话管理与参数调整](#s10)
11. [补关联、备份与训练交接](#s11)
12. [常见故障与处理顺序](#s12)
13. [验证命令和已知边界](#s13)
14. [一次采集的验收清单](#s14)

<a id="s1"></a>
## 1. 系统组成与本机路径

### 1.1 三个环境各自负责什么

| 组件 | 本机路径或解释器 | 作用 |
|---|---|---|
| PiperXR 项目 | `/home/wtc/picoxpiper/PiperXR-master` | 本手册、配置、统一启动脚本和采集扩展 |
| PiPER 控制环境 | `~/anaconda3/envs/pico_teleop/bin/python` | XR 输入、placo IK、CAN 控制、机械臂反馈 |
| RealSense 项目 | `~/realsense_src` | 复用已有 `scripts/camera.py` 的设备选择、流配置、标定和时间戳功能 |
| RealSense 解释器 | `~/realsense_src/.venv/bin/python` | D435 RGB-D 采集、深度处理、Open3D 点云导出 |
| EVA 项目 | `~/EVA-CLIENT` | 浏览器操作台、录制、LeRobot 格式数据、视频与质检 |
| EVA 解释器 | `~/EVA-CLIENT/.venv/bin/python` | 启动协调进程、采集桥接和 EVA 应用 |
| XRoboToolkit PC 服务 | `/opt/apps/roboticsservice/RoboticsServiceProcess` | 接收 PICO 的 XR 数据 |

以上路径区分大小写。你口头提到的 EVA client，在本机配置中的目录是 `~/EVA-CLIENT`。

在独立终端中执行 D435 检查时需要加载 `env.sh`；使用统一数采入口时，不需要手动逐个激活三个环境。启动脚本会选用各自的解释器。

### 1.2 数据如何流动

```text
PICO 的 XRoboToolkit 应用
    -> XRoboToolkit PC 服务
    -> PiperXR robot worker -> CAN -> PiPER
                  |
                  +-> 关节/夹爪反馈、实际发送的目标

D435 -> camera worker -> RGB、原始深度、对齐深度、标定参数
                  |
                  v
             collection bridge
                  |
                  +-> EVA -> Parquet、RGB MP4、任务与质检元数据
                  |
                  +-> raw_rgbd 原始 HDF5
                              |
                              +-> 与 EVA episode 对应的 rgbd HDF5
                                               |
                                               +-> PNG 深度图、彩色 PLY 点云
```

只有 robot worker 持有 CAN 控制连接。不要同时启动另一个真机 PiperXR、Piper ROS 驱动或其他向同一机械臂下发命令的程序。

本入口通过 `piper_xr/collection/eva_entry.py` 注册 `single_piper_d435` 机器人和 `piperxr` 传输扩展，随后启动 EVA。因此需要使用本项目的 `scripts/collect.sh`。直接对生成配置运行普通 `eva --config ...` 会缺少这些注册扩展。

### 1.3 三种不同的“启动”

| 操作 | 结果 |
|---|---|
| 启动 `collect.sh real` | 建立反馈与相机流，打开 EVA；初始运动授权为 `LOCKED`，连接本身不使能机械臂 |
| EVA 开启 `MOTION` | 授权遥操作；真机还要求有效 PICO 输入及按住 grip 才实际控制 |
| 点击 `START RECORD` | 开始保存一段演示；必须先选择任务并开启 MOTION |

录制开关与运动授权是两个独立状态。`END / SAVE` 停止保存当前段，但不会自动关闭 MOTION。关闭 MOTION 或退出程序也不意味着电机断电；该控制路径不会将其当作硬件急停。

<a id="s2"></a>
## 2. 日常操作速查

本节适用于已经完成一次实机验收的情况。首次使用请从第 3 节开始。

所有未注明位置的相对路径命令，都在以下目录执行：

```bash
cd ~/picoxpiper/PiperXR-master
```

1. 确认机械臂与 D435 接线、相机视野、工作区域和剩余磁盘空间。
2. 确认 `can0` 已按 1 Mbps 启动，且没有其他程序控制 PiPER 或占用 D435。
3. 启动 PC 服务，并在 PICO 的 XRoboToolkit 应用中连接电脑。
4. 启动真机数采，浏览器打开 `http://127.0.0.1:8080`。
5. 选择数据集与任务，开启 MOTION，用 grip + trigger 完成遥操作。
6. 每段使用 `START RECORD` 开始，`END / SAVE` 保存；错误演示用 `CANCEL` 放弃。
7. 检查回放和保存结果；结束时释放 grip、保存当前段、关闭 MOTION，再在采集终端按 Ctrl+C。

终端 A：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/start_pc_service.sh
```

终端 B：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh real
```

也可以在 PC 服务尚未运行时，由采集启动器一并启动：

```bash
bash scripts/collect.sh real --start-pc-service
```

这两种 PC 服务启动方式选择一种，避免重复启动。

<a id="s3"></a>
## 3. 首次使用：检查环境与配置

### 3.1 环境配置

数采由三个独立的 Python 环境和一个 XRoboToolkit PC 服务组成。当前电脑的环境已经存在，日常使用直接从第 3.2 节的 `check` 开始；第 3.1.2 节用于新机器部署或确实缺失环境时的重建。各环境的 Python 和 NumPy 版本不同，不合并安装。统一入口及点云、CAN 工具的 shell 包装脚本会选择各自解释器，无需在一个终端连续激活三个环境。

#### 3.1.1 固定路径与职责

| 环境 | 解释器 | 负责内容 | 关键依赖/输出 |
|---|---|---|---|
| Piper 控制环境 | `~/anaconda3/envs/pico_teleop/bin/python` | XRoboToolkit 输入、placo IK、PiPER SDK、CAN 反馈和控制 | `numpy`、`placo`、`meshcat`、`piper_sdk`、`python-can`、`xrobotoolkit_sdk`、`xrobotoolkit_teleop` |
| RealSense 环境 | `~/realsense_src/.venv/bin/python` | D435 设备访问、RGB-D 帧、深度和点云 | `pyrealsense2`、`opencv`、`open3d`、`h5py`、`numpy` |
| EVA 环境 | `~/EVA-CLIENT/.venv/bin/python` | EVA 页面、ZMQ 桥接、LeRobot/Parquet、视频、质量和 RGB-D 关联 | `pyarrow`、`h5py`、`imageio`、`pyzmq`、`openpi-client`、`pyroki` |
| XRoboToolkit PC 服务 | `/opt/apps/roboticsservice/RoboticsServiceProcess` | 接收 PICO 头显和手柄数据，默认端口 `60061` | C++ 运行库、服务自身的 `lib/`、`SDK/x64/` |

本项目目录为 `~/picoxpiper/PiperXR-master`；配置中的 `eva_root`、`realsense_root` 和 `piper_python` 必须分别指向上表的项目/解释器。`read_settings()` 会把相对路径解析到 PiperXR 项目根目录，`~` 会展开为当前用户主目录。

2026-09-17 从本机解释器和包元数据读取的版本如下。这是当前安装记录，不表示任意新版组合已通过验证；本项目根目录 `requirements.txt` 中的部分旧版本与当前环境不同，不能直接将它当作当前环境的完整锁定文件。

| 环境 | Python | 本机关键包版本 |
|---|---|---|
| 系统 | Ubuntu 22.04.5 LTS | Linux x86_64；无需安装 ROS1/ROS2 |
| Piper | `3.10.21` | `piper_sdk 0.6.2`、`python-can 4.6.1`、`placo 0.9.25`、`numpy 2.2.6`、`xrobotoolkit-sdk 1.0.2`、`xrobotoolkit-teleop 1.0.3` |
| RealSense | `3.10.12` | `pyrealsense2 2.58.4.10922`、`numpy 1.26.4`、`opencv-contrib-python 4.11.0.86`、`open3d-cpu 0.19.0`、`h5py 3.16.0` |
| EVA | `3.11.16` | `numpy 1.26.4`、`h5py 3.13.0`、`pyarrow 23.0.0`、`pyzmq 27.2.0`、`imageio 2.37.3`、`imageio-ffmpeg 0.6.0` |

EVA 的 `pyproject.toml` 要求 `>=3.11,<3.12`。PiPER 的 Python 原生绑定使用本机 Python 3.10 环境。依赖缺失时，应使用相应解释器的 `-m pip`，或给 `uv pip` 显式传入 `--python`，避免安装到 Conda base。

#### 3.1.2 第一次安装或环境损坏时

以下命令假定三个项目源码已放在第 1 节路径中；当前电脑无需重复安装。新电脑先准备 Conda、`uv`、编译工具与网络，并保持 Piper 使用 CPython 3.10、EVA 使用 CPython 3.11、RealSense 使用系统 CPython 3.10。Ubuntu 基础工具缺失时可在本地终端安装：

```bash
sudo apt-get update
sudo apt-get install -y git cmake build-essential iproute2 usbutils python3.10-venv
```

Conda 安装在本机 `~/anaconda3`；新机器路径不同时，后续 JSON 和包装脚本变量需同步调整。EVA 安装依赖 `uv`，可按 [EVA 安装说明](../../EVA-CLIENT/docs/installation.md)准备。源码编译还可能要求对应项目列出的开发库，遇到缺失时按具体构建错误补齐。

**Piper / XR 环境。** 项目 `setup_env.sh` 创建或复用 `pico_teleop`，下载第三方源码、编译 XRoboToolkit C++/Python 绑定、安装 `xrobotoolkit_teleop` 和本项目。它会重新复制 `assets/piper/piper.xml` 与网格并生成双臂模型，因此已有模型定制时应先保留副本，不能把这个脚本当作每次采集前的检查命令。它也不安装完整的 XRoboToolkit PC 服务应用。

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/setup_env.sh
~/anaconda3/envs/pico_teleop/bin/python -m pip install piper_sdk==0.6.2 python-can==4.6.1
```

最后一行补充真机 CAN 依赖，`setup_env.sh` 没有显式安装它们。脚本会复用已有第三方源码，但部分下载指向上游分支；要精确复现本机，应同时保留 `third_party/` 源码版本及依赖清单，不能仅依赖未来某次重新下载。

**EVA 环境。** 仅当 `.venv` 不存在时创建，然后安装当前项目依赖；`dev` 包含本手册软件验证使用的 pytest 等工具：

```bash
cd ~/EVA-CLIENT
test -d .venv || uv venv --python 3.11 .venv
uv pip install --python .venv/bin/python -e '.[dev]'
```

这里采用 EVA 项目自己的安装方式，不需要将 Piper/XR 或 RealSense 的依赖安装到 EVA。`pyroki`、`jaxls` 等依赖使用项目指定的 Git 版本，安装时需要能访问对应仓库；导入路径由项目安装及数采入口负责。

**RealSense 环境。** 仅在 `.venv` 不存在时创建，显式使用 Ubuntu 的 Python 3.10，避免当前 Conda 环境改变 `python3` 的含义：

```bash
cd ~/realsense_src
test -d .venv || /usr/bin/python3.10 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
```

`requirements.txt` 定义主要依赖；已有的 `requirements.lock.txt` 是 RealSense 项目先前记录的完整安装清单，可用于同平台重建，但不自动随本机后续安装更新。不要在此环境同时安装多个提供 `cv2` 的 OpenCV wheel；本机使用 `opencv-contrib-python`。

本地 C++ librealsense SDK 位于 `~/realsense_src/sdk/`，已有构建为 RSUSB 后端，使用 libusb，不依赖 ROS2。Python `pyrealsense2` wheel 与 C++ SDK/Viewer 是不同安装部分，`pip install` 不会重建 `sdk/`。若需要重建 C++ SDK，按 [RealSense 软件自检与重建说明](../../realsense_src/README.zh-CN.md#软件自检与重建)执行；保留 `env.sh` 和 `scripts/camera.py`，因为数采入口会复用后者。USB 规则安装及实际相机检查见第 5.2 节。

这些重建命令只写入文档，本次没有重新安装环境。当前环境可用时，直接进行下一节的导入自检。

#### 3.1.3 不启动硬件的环境自检

以下命令只导入模块，不连接 D435、CAN、PICO 或机械臂。每段使用子 shell，RealSense 的环境变量不会留在后续命令中：

```bash
cd ~/picoxpiper/PiperXR-master

(~/anaconda3/envs/pico_teleop/bin/python -c "import numpy, placo, meshcat, can, piper_sdk, xrobotoolkit_sdk, xrobotoolkit_teleop; print('Piper runtime OK')")

(
  source ~/realsense_src/env.sh
  python -c "import numpy, h5py, cv2, pyrealsense2, open3d; print('RGB-D runtime OK')"
)

(
  cd ~/EVA-CLIENT
  PYTHONPATH="$PWD/src" .venv/bin/python -c "import core.config, openpi_client, pyarrow, zmq; print('EVA runtime OK')"
)
```

`source ~/realsense_src/env.sh` 会在当前终端设置 RealSense 的 `PATH`、`LD_LIBRARY_PATH`、`CMAKE_PREFIX_PATH` 和 `PKG_CONFIG_PATH`。相机检查完成后可关闭该终端；正式统一入口会在 camera worker 中单独传入 SDK 库路径，不要求在启动采集前手动激活 RealSense 环境。

#### 3.1.4 环境变量和脚本行为

| 变量 | 由谁使用 | 作用 |
|---|---|---|
| `PYTHONPATH` | `collect.sh`、诊断和点云脚本 | 自动加入 PiperXR 项目目录；通常不需要手动设置 |
| `EVA_ROOT` | `collect.sh`、EVA 入口 | `collect.sh` 用它选择 `.venv/bin/python`，默认 `~/EVA-CLIENT`；JSON 的 `eva_root` 决定 EVA 源码/基础配置路径，迁移时两者必须一致 |
| `REALSENSE_ROOT` | `scripts/export_pointcloud.sh` | 选择 RealSense 项目根目录，默认 `~/realsense_src` |
| `PIPER_PYTHON` | CAN 诊断和角色恢复脚本 | 覆盖默认 Piper 解释器；正式采集使用 JSON 的 `piper_python` |
| `XR_SERVICE_DIR` | `scripts/start_pc_service.sh` | XRoboToolkit PC 服务目录，默认 `/opt/apps/roboticsservice` |
| `XR_CXX_RUNTIME` | `scripts/start_pc_service.sh` | `libstdc++.so.6` 路径，默认 Conda 环境中的运行库 |
| `LD_PRELOAD`、`QT_PLUGIN_PATH`、`QT_QML_PATH` | `scripts/start_pc_service.sh` | 脚本为服务进程选择 C++ 运行库及服务自带 Qt 资源，无需永久写入 `.bashrc` |
| `LD_LIBRARY_PATH` | RealSense 脚本、PC 服务 | 分别用于本地 RealSense SDK 或 XR 服务动态库；不要把两套不兼容的库路径混在同一个手动命令里 |

统一入口启动的 launcher、bridge 和 EVA 使用 `collect.sh` 选中的 EVA Python；robot worker 使用 JSON 的 `piper_python`；camera worker 使用 `realsense_root/.venv/bin/python`，并将该子进程的 `LD_LIBRARY_PATH` 设为 `realsense_root/sdk/lib`。源配置更改后应重启采集。硬件准备完成后，正式启动从项目入口执行：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh real
```

迁移目录时同时修改 JSON 和对应脚本变量。例如 JSON 的 `eva_root` 已改为 `/data/EVA-CLIENT` 后，用以下方式选择同一路径的解释器：

```bash
EVA_ROOT=/data/EVA-CLIENT bash scripts/collect.sh check --settings "$PWD/configs/collection_session_01.json"
```

其中 `/data/EVA-CLIENT` 是示例路径，必须已安装好环境。`PIPER_PYTHON` 仅供诊断/恢复包装脚本使用，不能替代正式采集 JSON 的 `piper_python`；`REALSENSE_ROOT` 同理只影响点云包装脚本，正式采集读取 JSON 的 `realsense_root`。

#### 3.1.5 PC 服务、硬件权限和端口

Python 能导入 `xrobotoolkit_sdk` 不代表 PC 服务已安装或 PICO 已连接。当前服务启动脚本依赖以下两个已有文件，可先检查是否存在：

```bash
ls -l /opt/apps/roboticsservice/RoboticsServiceProcess
ls -l ~/anaconda3/envs/pico_teleop/lib/libstdc++.so.6
```

服务安装目录不存在时，需要按 [XRoboToolkit PC Service 项目说明](https://github.com/XR-Robotics/XRoboToolkit-PC-Service)安装适配本机系统的 PC 服务。`setup_env.sh` 编译的 Python SDK 不能代替完整服务应用。服务位于其他目录时设置 `XR_SERVICE_DIR`；运行库路径不同时设置 `XR_CXX_RUNTIME`，然后仍通过 `scripts/start_pc_service.sh` 启动，以保留其动态库和 Qt 配置。

本机检查：软件导入通过 → D435 USB3/udev 与独立帧检查 → CAN UP / 1 Mbps 和关节/夹爪反馈 → PC 服务与 PICO 连接 → 启动数采。具体硬件命令集中在第 5 节，CAN 异常见第 12 节。默认 EVA 页面 `8080`、控制通道 `5757`、ZMQ `5555`～`5557` 和 XR PC 服务 `60061` 见第 3.4 节；PICO 使用电脑局域网地址，采集 ZMQ 保持 `127.0.0.1`。

#### 3.1.6 环境问题的定位

| 报错 | 对应处理 |
|---|---|
| 解释器路径不存在 | 核对 `.venv`/Conda 是否创建及 JSON 路径，不将系统 Python 临时替换进去 |
| `No module named piper_sdk` / `can` | 检查 `pico_teleop`，按第 3.1.2 节补充 CAN 包 |
| `No module named xrobotoolkit_sdk` / `.so` 导入失败 | 检查 Python 3.10 绑定、SDK 编译产物与动态库；安装其他环境的同名包不能修复此环境 |
| `No module named pyrealsense2` / `open3d` / `cv2` | 检查 RealSense `.venv` 和其 `requirements.txt`；包名 `open3d-cpu` 导入时写 `open3d` |
| EVA 提示 Python 版本不支持 | 使用 EVA `.venv` 的 Python 3.11，检查 `EVA_ROOT` 与 JSON 是否一致 |
| `GLIBCXX_* not found` 或 PC 服务 Qt 插件失败 | 使用 PC 服务包装脚本，核对 `XR_CXX_RUNTIME` 和服务目录；避免全局混入其他环境的 Qt 库 |
| `check` 通过，但 D435/CAN 启动失败 | 转到硬件检查；导入检查不证明 USB 权限、相机帧或 CAN 测量反馈可用 |

### 3.2 软件检查

完成环境检查后，使用正式采集相同的配置执行项目级检查：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh check
```

项目级检查会用 JSON 指定的 Piper 与 RealSense 解释器导入模块，并在启动器所用的 EVA 解释器中加载单臂配置。它仍不会打开相机、CAN、PICO 或电机。

预期输出包含：

```text
Piper runtime OK
RGB-D runtime OK
EVA configuration OK: ...; robot=single_piper_d435
Configuration check only; camera/CAN/PICO and motion were not started.
```

该命令检查所需模块、生成 EVA 配置并验证配置加载，不连接 D435、不启动 PICO 控制、不使能机械臂。检查通过只能证明软件环境和配置能够加载。

生成文件默认位于：

```text
work_dirs/collection/single_piper_d435/eva_single_piper_d435.py
```

日常修改应写入 JSON 源配置。生成的 Python 配置会在后续启动时重新生成，不适合手工维护。

### 3.3 当前完整配置

配置文件：[configs/collection_single_d435.json](configs/collection_single_d435.json)。下面内容与本手册编写时的文件一致：

```json
{
  "eva_root": "~/EVA-CLIENT",
  "realsense_root": "~/realsense_src",
  "piper_python": "~/anaconda3/envs/pico_teleop/bin/python",
  "output_dir": "work_dirs/collection/single_piper_d435",
  "web_port": 8080,
  "obs_endpoint": "tcp://127.0.0.1:5555",
  "action_endpoint": "tcp://127.0.0.1:5556",
  "rpc_endpoint": "tcp://127.0.0.1:5557",
  "control_port": 5757,
  "fps": 30,
  "max_robot_skew_s": 0.06,
  "image_skew_tolerance_s": 0.05,
  "camera": {"serial": "", "width": 640, "height": 480},
  "robot": {
    "can": "can0", "hand": "right", "scale": 0.7,
    "max_dq": 0.02, "speed": 20,
    "gripper_open_m": 0.08, "gripper_effort": 1000
  },
  "tasks": {"pick_place": [["pick up the object and place it in the tray", -1]]}
}
```

**帧率字段是顶层 `fps`，不是 `camera.fps`。** 当前使用同一个值配置 D435 流和 EVA 保存帧率。宽、高和帧率必须构成 D435 支持的 RGB + Z16 流组合。

### 3.4 常用参数说明

| 参数 | 默认值 | 含义与调整依据 |
|---|---|---|
| `robot.can` | `can0` | 机械臂实际 CAN 接口；脚本不会自动改系统 CAN 配置 |
| `robot.hand` | `right` | 使用右手柄；可改 `left`，不会因此变为双臂数据 |
| `robot.scale` | `0.7` | 手柄位移到末端位移的缩放 |
| `robot.max_dq` | `0.02` | 每控制周期的关节目标限幅，单位 rad；不是录制帧率 |
| `robot.speed` | `20` | PiPER SDK 的速度比例数值；不是 20 rad/s |
| `robot.gripper_open_m` | `0.08` | 最大夹爪开口，单位 m；按实际夹爪标定，0 表示闭合 |
| `robot.gripper_effort` | `1000` | SDK 夹爪力矩指令值，单位 0.001 N·m；不等同于指尖夹持力 |
| `camera.serial` | 空字符串 | 单台 D435 可留空；要固定设备时填序列号 |
| `camera.width` / `camera.height` | `640` / `480` | 彩色与深度请求分辨率 |
| `fps` | `30` | 相机请求帧率与 EVA 保存帧率 |
| `max_robot_skew_s` | `0.06` | 机器人与相机接收样本配对容差，秒；录制中超限会报错停止桥接 |
| `image_skew_tolerance_s` | `0.05` | EVA 时间网格选图及 RGB-D 补关联的允许误差，秒 |
| `output_dir` | `work_dirs/collection/single_piper_d435` | 相对路径从 PiperXR 项目根目录解析；支持绝对路径和 `~` |
| `tasks` | `pick_place` | 数据集名到任务列表的映射；每项为 `[描述, 目标段数]`，`-1` 表示不限数量 |

控制 worker 的循环目标约为 50 Hz，实际频率受计算和通信耗时影响；数据集保存默认 30 FPS，两者不要求一一等频。

本机默认端口如下：

| 端口 | 用途 |
|---|---|
| `8080` | EVA 浏览器操作台 |
| `5555` | 桥接发布观测 |
| `5556` | EVA 传输配置保留的动作通道；本配置不接受 EVA 策略或手动动作控制真机 |
| `5557` | 桥接 RPC，包含运动授权与录制控制 |
| `5757` | EVA 控制通道 |
| `60061` | XRoboToolkit PC 服务，独立于上述数采 JSON |

三个 ZMQ endpoint 当前要求 `tcp://127.0.0.1:端口`。不要把 PICO 的局域网连接地址填进这些字段。

### 3.5 使用自定义配置

建议为不同采集会话复制一份 JSON，再编辑输出目录、任务与必要参数：

```bash
cp configs/collection_single_d435.json configs/collection_session_01.json
```

编辑完成后，使用同一份配置执行检查、启动和补关联：

```bash
bash scripts/collect.sh check --settings "$PWD/configs/collection_session_01.json"
bash scripts/collect.sh real --settings "$PWD/configs/collection_session_01.json"
```

以上真机启动命令应在第 5 节硬件准备完成后运行。传给 `--settings` 的路径建议使用绝对路径，避免父进程和子进程的工作目录不同。

若迁移 EVA 目录，要同时修改 JSON 的 `eva_root` 和启动脚本使用的 `EVA_ROOT` 环境变量，使解释器与源码来自同一个 EVA 项目。点云脚本使用 `REALSENSE_ROOT` 环境变量选择环境；迁移 RealSense 目录时也需要同步设置。

<a id="s4"></a>
## 4. 先完成一次纯模拟录制

### 4.1 启动模拟

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh mock
```

打开 <http://127.0.0.1:8080>。模拟模式使用合成机械臂状态与 RGB-D 数据，不连接机械臂、D435 或 PICO；仍需本机已有的软件环境。

模拟数据自动保存到真实输出目录加 `_mock` 的独立位置：

```text
work_dirs/collection/single_piper_d435_mock/
```

HDF5 中会记录 `synthetic=True`。模拟数据用来验证流程，不应混入真实训练集。

### 4.2 完成“保存、取消、再保存”

1. 进入 `COLLECT`，在 `DATASET NAME` 中选择 `pick_place`。
2. 选择对应的任务描述，例如 `pick up the object and place it in the tray`。
3. 将 `MOTION` 从 `LOCKED` 切换为 `ENABLED`；模拟动作随授权开始变化。
4. 点击 `START RECORD`，等待约 5 秒，再点击 `END / SAVE`。
5. 等待后台保存与转换结束，选中生成的 episode，检查视频回放和关节变化。
6. 再次开始一段录制，约 3 秒后点击 `CANCEL`；该段应不生成正式训练 episode。
7. 再录约 5 秒并保存，检查第二段正式数据。
8. 关闭 MOTION，在启动终端按 Ctrl+C，等待后台处理与子进程退出。

取消段的 `raw_rgbd` 暂存仍会保留用于排错，因此原始 HDF5 数量可以多于最终 episode 数量。

如保存结束后尚未看到对应 `rgbd` 文件，等转换结束后执行：

```bash
bash scripts/collect.sh link --mock-data
```

随后可以将第 8、9 节示例路径中的 `single_piper_d435` 换为 `single_piper_d435_mock`，检查模拟数据并导出点云。

<a id="s5"></a>
## 5. 真机准备：D435、CAN、PICO

### 5.1 场景与磁盘

固定机械臂和相机，确认相机同时能看到夹爪、物体与目标区域。相机安装位置发生变化后，原有相机到机械臂外参不能继续直接沿用。

首次试采使用空旷工作区域和小幅动作，预先确认机械臂的硬件急停方式。MOTION、松开 grip 和软件断线保持都不等同于硬件急停。

检查保存盘：

```bash
df -h .
du -sh work_dirs/collection
```

若使用自定义绝对输出目录，应对相应磁盘运行 `df -h`。目录尚未创建时，`du` 提示不存在是正常的。

640 × 480 × 30 FPS 的 RGB 加两份 uint16 深度，其未压缩像素量约为 64.5 MB/s。实际 HDF5 使用 LZF 无损压缩，但还会保留原始归档、episode RGB-D 副本和视频，不能按视频大小估计总占用。建议使用本地 SSD，并先以短段测量实际写入速度和空间增长。

### 5.2 D435 独立检查

先关闭 RealSense Viewer、原来的独立录制程序以及其他占用 D435 的进程。集成采集与原来的独立录制不能同时占用同一台相机。

在一个单独终端执行：

```bash
source ~/realsense_src/env.sh
python ~/realsense_src/scripts/camera.py check --require-camera --frames 60
```

此步骤会连接真实相机并读取帧。确认能识别设备、采集 RGB 与深度，且没有流配置或 USB 错误，然后让检查正常结束。

查看 USB 连接速率：

```bash
lsusb -t
```

使用支持 USB 3 的线缆和接口，优先直连电脑；USB 拓扑通常应显示 `5000M` 或更高的连接速率。当前采集 worker 选择设备时不允许 USB 2 降级。

只有在检查提示系统规则或权限依赖缺失时，再参考 RealSense 项目执行系统安装脚本：

```bash
bash ~/realsense_src/scripts/setup-system.sh --with-tools
```

该脚本可能请求管理员权限；按脚本提示重新插拔设备后复查。不要用重新安装整个环境代替对具体报错的检查。

### 5.3 PiPER CAN 检查

确认机械臂电源、通信线和 USB-CAN 适配器接好，确认接口名称：

```bash
ip -brief link
ip -details link show can0
```

正常采集要求对应 CAN 接口处于可用状态，bitrate 为 `1000000`。只有确认 `can0` 就是这台 PiPER 的适配器，并且尚未正确配置时，再执行：

```bash
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up
ip -details link show can0
```

这些命令会改变实际 CAN 接口状态。如果实际设备名不是 `can0`，应同时替换命令中的名称和 JSON 的 `robot.can`。

关闭其他真机遥操作、Piper SDK 控制示例和 ROS 控制节点。不要为了确认连接而并行启动第二个机械臂控制程序。

### 5.4 PICO 与 PC 服务

终端 A：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/start_pc_service.sh
```

在头显中：

1. 打开 XRoboToolkit-PICO-Client 应用。
2. 让 PICO 与电脑位于可互通的局域网，按应用界面连接这台电脑。
3. 确认手柄追踪有效，当前配置使用右手柄。
4. 开启 MOTION 前松开 grip，避免授权后立刻进入运动控制。

这里的 PICO 输入来自 XRoboToolkit。EVA WebXR 的 A/B 录制快捷键不适用于这一输入链路；使用 EVA 网页录制按钮，或网页当前显示的键盘快捷键。

<a id="s6"></a>
## 6. 逐步完成一段真机数据采集

### 6.1 启动采集进程

在终端 B：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh real
```

打开 <http://127.0.0.1:8080>。启动过程中 D435 会预热，启动器等待相机与机器人就绪；如终端报错，先查看最早的 worker 错误。

确认网页中的相机画面持续更新、机械臂状态合理、MOTION 为 LOCKED。图像入口命名为 `cam_high`，只是数据字段名，并不要求 D435 一定安装在正上方。

### 6.2 选择数据集和任务

在 `COLLECT` 中选择 `DATASET NAME = pick_place`，再选择任务描述。任务文本应准确描述当前演示，例如“抓起物体并放入托盘”。

每一段 episode 应包含一个边界清晰的任务尝试。回到初始位、重新摆物体、等待操作者准备等动作，通常放在录制开始前，以免把无关操作混入演示。

### 6.3 授权运动并熟悉遥操作

1. 确认场景和反馈正常，手柄 grip 处于释放状态。
2. 打开 EVA 的 MOTION。
3. 轻握并按住右手柄 grip，首次使能可能需要等待反馈；有效输入和授权都满足后，机械臂开始跟随。
4. 小幅平移手柄，确认方向、位移缩放与机器人响应。
5. 在按住 grip 时操作 trigger，确认夹爪开合方向与最大开口。
6. 松开 grip，确认关节目标保持到当前反馈位置。
7. 再次按住 grip 会重新捕获参考位姿，便于调整手柄位置后继续操作。

grip 使用高阈值判定，需要按到位。当前采集入口在激活时保留实际末端朝向，不使用原遥操作路径中的自动朝下基准。

当 MOTION 关闭、PICO 数据失效或授权心跳超时，控制路径会释放遥操作控制。在通信正常且反馈有效时尝试保持当前位置；不能把网络故障后的软件行为视为机械硬件停止保证。

本配置只接受 PiperXR 的 PICO 遥操作。EVA 的 `HOME`、手动关节控制和策略推理动作不会转发到 CAN。需要回到起始位置时，在下一段录制前用 PICO 遥操作完成。

### 6.4 首次进行 10–20 秒短段试采

1. 场景和起始位准备好后，点击 `START RECORD`。
2. 做少量平移、少量转腕和一次夹爪开合，观察有无跳变、追踪中断或图像冻结。
3. 完成动作后释放 grip，点击 `END / SAVE`。
4. 等待 EVA 保存与转换队列完成；RGB-D 关联文件可能比界面中的视频稍晚出现。
5. 回放 episode，确认任务动作与图像一致。
6. 按第 8 节检查 Parquet 和 HDF5，并按第 9 节导出一帧深度与点云。
7. 通过短段验收后，再开始批量采集。

配置中的最小有效段长为 10 帧。很快开始又停止的片段可能达不到有效帧数要求；首次试采不要用极短录制判断保存是否正常。

### 6.5 批量录制与质检

每段开始前恢复场景，选择正确任务，确保 MOTION 和数据流正常。点击 `START RECORD` 后完成一次任务，再点击 `END / SAVE`。

如果当前段明显不适合作为演示，点击 `CANCEL`。取消不会生成该段正式训练 episode，但仍可能保留 raw RGB-D 暂存。

选中保存后的 episode 检查视频与轨迹，并在 EVA 的质检界面标记 PASS 或 FAIL。建议记录失败原因，例如抓取滑落、遮挡、追踪丢失、任务未完成或同步异常。自动质量状态与人工任务成功标注不是同一件事。

如果保存队列已满，应等待后台处理，不要连续发起新录制。当前生成配置的保存队列上限为 4。

### 6.6 正常结束

1. 释放手柄 grip。
2. 若仍在录制，点击 `END / SAVE`，或明确用 `CANCEL` 放弃当前段。
3. 等待保存与转换队列结束，确认最新 episode 可见。
4. 关闭 MOTION，使其显示 LOCKED。
5. 在启动采集的终端按一次 Ctrl+C，等待进程清理、RGB-D 最终关联及退出完成。
6. 手动在另一个终端启动的 PC 服务，可在不再使用时单独 Ctrl+C；启动器代为启动的服务会由其管理。
7. 最后按机械臂操作规程处理电机、急停和断电；不要把进程退出理解为自动电机断使能。

浏览器关闭不等同于采集进程停止。不要用关闭网页作为结束数采的唯一操作。

<a id="s7"></a>
## 7. 数据目录、字段、单位和时间戳

### 7.1 默认输出目录

```text
work_dirs/collection/single_piper_d435/
├── eva_single_piper_d435.py
├── logs/
│   └── bridge_<启动时间>_<进程号>.log
├── raw_rgbd/
│   └── capture_<uuid>.h5
└── pick_place/
    └── raw/
        ├── data/chunk-000/episode_000000.parquet
        ├── videos/chunk-000/observation.images.cam_high/episode_000000.mp4
        ├── rgbd/chunk-000/episode_000000.h5
        ├── rgbd/chunk-000/episode_000000.json
        └── meta/
            ├── info.json
            ├── episodes.jsonl
            ├── episodes_stats.jsonl
            ├── tasks.jsonl
            └── stats.json
```

上面列出主要训练文件；运行时还可能产生缓存、日志、质检及转换中间文件。`pick_place` 对应配置中的数据集名，episode 编号由 EVA 管理，后续数据可能位于其他 chunk。

| 文件 | 用途 |
|---|---|
| `data/...parquet` | EVA 对齐后的状态、动作、末端位姿、时间和索引 |
| `videos/...mp4` | EVA 的 RGB 视频 |
| `rgbd/...h5` | 与该 episode 逐帧对应的 RGB、无损深度、原始配对样本和标定 |
| `rgbd/...json` | 关联来源、帧数、最大图像时间误差和深度编码说明 |
| `raw_rgbd/capture_*.h5` | 录制期间桥接接收的原始 RGB-D 归档及 EVA 实际消费帧清单 |
| `meta/` | 数据集定义、任务、episode 清单和统计信息 |
| `logs/bridge_*.log` | 各次启动的 bridge、相机 worker 与机器人 worker 输出，保留原始报错 |

本集成入口将深度保存为 HDF5，不以 ROS bag / `.db3` 为主存储。原 RealSense 项目的独立录制功能仍可单独使用，但不能与本入口同时占用相机。

### 7.2 状态与动作

| Parquet 字段 | 每帧维度 | 内容 |
|---|---|---|
| `observations.state.qpos` | 7 | 实测状态：6 个关节角度 + 夹爪开口 |
| `action.qpos` | 7 | 关节绝对目标 + 夹爪目标开口 |
| `observations.state.eef` | 8 | 由状态经 FK 计算的末端位置、四元数、夹爪开口 |
| `action.eef` | 8 | 由动作目标经 FK 计算的末端位置、四元数、夹爪开口 |

7 维顺序为 `[joint1, joint2, joint3, joint4, joint5, joint6, gripper]`。关节单位是 rad，夹爪单位是 m；夹爪数值不是 0–1 归一化闭合量。

动作来自控制端经过限幅和 SDK 整数单位转换后实际发送的目标。保持期间延续最后目标；尚未使能、没有发送目标时，以当前反馈初始化动作。因此动作并不表示“每一行都重新向 CAN 发送了一次命令”，也不保证等于同一时刻的实测关节位置。

8 维末端顺序为 `[x, y, z, qw, qx, qy, qz, gripper]`，位置单位 m，四元数为 **wxyz**。EVA FK 使用 URDF 的 `gripper_base`，PiperXR 遥操作使用 `link6`；二者是不同的末端参考点。该位姿为运动学计算结果，不是额外传感器测得的位姿。

### 7.3 RGB-D HDF5

| 数据集 | 类型或维度 | 含义 |
|---|---|---|
| `rgb` | uint8，`N × H × W × 3` | RGB 通道顺序的彩色原图 |
| `depth_raw` | uint16，`N × H × W` | 原生深度相机像素网格 |
| `depth_aligned` | uint16，`N × H × W` | 对齐到彩色像素网格的深度 |
| `state` / `action` | `N × 7` | 与源相机帧配对的机器人原始样本 |
| `capture_time` | `N`，秒 | episode 文件中与 Parquet 对应的对齐时间 |
| `source_capture_time` | `N`，秒 | 被选择的源相机帧在主机接收的单调时钟时间 |
| `image_skew_s` | `N`，秒 | `source_capture_time - capture_time` |
| `frame_index` | `N` | 对应 Parquet 行的帧索引 |
| `source_frame_index` | `N` | 在原始归档中的索引，可能重复或跳过 |
| `robot_time` / `state_time` / `action_time` | `N`，秒 | 机器人 worker 相关采样时刻 |
| `depth_frame_number` / `color_frame_number` | `N` | 相机的原始帧号 |
| `depth_timestamp_ms` / `color_timestamp_ms` | `N`，毫秒 | 相机原始时间戳，解释时需同时读取其时间域 |
| `host_unix_ns` | `N`，纳秒 | 主机 Unix 时间，用于关联墙上时间 |

上表以最终 `rgbd` episode 为准。`raw_rgbd` 归档的 `capture_time` 是源接收时刻，并额外保存 `eva_capture_time` 消费清单；它没有最终 episode 的全部关联字段。

HDF5 的 `state` 和 `action` 是源帧配对样本，不能默认与 Parquet 经过时间对齐后的浮点值逐项完全相等。训练使用的状态、动作应以 Parquet 定义为准，深度按 episode 和 `frame_index` 与之关联。

属性 `calibration` 保存 JSON 格式标定信息，包括 `depth_scale_meters`、深度/彩色内参、对齐深度内参、畸变、深度到彩色外参。`settings` 保存采集配置；其他属性包含时间域、原始归档完成状态和 `synthetic` 等信息。

深度数值转米：`depth_m = depth_uint16 * calibration["depth_scale_meters"]`。零表示无效深度，不表示表面距离为 0 m。不要固定假设比例必然为 0.001，应使用文件里的实际标定值。

### 7.4 时间对齐的含义

机器人和相机首先按主机单调接收时钟配对，EVA 再生成固定帧率时间网格。深度关联只从 EVA 实际消费过的图像帧中选取与其一致的最近帧，因此原始归档中的所有帧不会无条件进入最终 episode。

`source_frame_index` 重复可能来自重采样复用同一相机帧，跳号可能来自未被选中的源帧；都需要结合帧率和误差判断。不能简单用原始帧数等于最终帧数作为验收条件。

这是软件接收时间对齐，不是相机曝光与关节反馈的硬件同步。CAN 关节与夹爪反馈也不是严格原子同步帧。`action_time` 记录 worker 的采样时刻，保持期间不能将其解读为一条新命令的准确硬件发出时刻。

单调时钟数值在重启后不可直接比较。归档还保存 Unix 时间、启动标识等信息，关联逻辑使用 episode 的墙上时间辅助排除其他启动周期的归档。后续分析不要只用一个裸的单调时间作为跨会话唯一标识。

<a id="s8"></a>
## 8. 检查一段数据是否完整

### 8.1 先检查文件

等待保存完成后，在项目目录执行：

```bash
ls -lh work_dirs/collection/single_piper_d435/pick_place/raw/data/chunk-000/
ls -lh work_dirs/collection/single_piper_d435/pick_place/raw/videos/chunk-000/observation.images.cam_high/
ls -lh work_dirs/collection/single_piper_d435/pick_place/raw/rgbd/chunk-000/
```

同一个 episode 应有 Parquet、MP4、RGB-D HDF5 及关联 JSON。目录或文件暂时不存在时，先查看保存队列与终端日志；不要马上重录或删除原始数据。

### 8.2 读取并核对 Parquet 与 HDF5

以下命令只读数据，不连接硬件。默认检查第一段；检查其他段时同时替换两个路径中的编号。使用 mock 或自定义配置时替换输出根目录。

```bash
~/EVA-CLIENT/.venv/bin/python - \
  work_dirs/collection/single_piper_d435/pick_place/raw/data/chunk-000/episode_000000.parquet \
  work_dirs/collection/single_piper_d435/pick_place/raw/rgbd/chunk-000/episode_000000.h5 <<'PY'
import json
import sys

import h5py
import numpy as np
import pyarrow.parquet as pq

table = pq.read_table(sys.argv[1])
with h5py.File(sys.argv[2], "r") as data:
    n = table.num_rows
    assert n >= 10, f"Too few frames: {n}"
    assert bool(data.attrs.get("complete", False)), "Archive not marked complete"
    assert len(data["capture_time"]) == n, "Frame count mismatch"
    for key in ("rgb", "depth_raw", "depth_aligned", "state", "action"):
        assert len(data[key]) == n, f"Wrong frame count: {key}"
    assert data["rgb"].dtype == np.dtype("uint8")
    for key in ("depth_raw", "depth_aligned"):
        assert data[key].dtype == np.dtype("uint16"), f"Wrong depth dtype: {key}"
    np.testing.assert_allclose(
        data["capture_time"][:], np.asarray(table["capture_time"].to_pylist()),
        rtol=0, atol=1e-9,
    )
    np.testing.assert_array_equal(data["frame_index"][:], table["frame_index"].to_pylist())
    for key, width in (
        ("observations.state.qpos", 7), ("action.qpos", 7),
        ("observations.state.eef", 8), ("action.eef", 8),
    ):
        values = np.asarray(table[key].to_pylist())
        assert values.shape == (n, width), (key, values.shape)
        assert np.isfinite(values).all(), f"Non-finite values: {key}"
    # Older sidecars may predate the settings attribute; use the configured
    # default tolerance only for this structural check.
    settings = json.loads(data.attrs.get("settings", "{}"))
    calibration = json.loads(data.attrs["calibration"])
    skew = float(np.max(np.abs(data["image_skew_s"][:])))
    assert skew <= settings.get("image_skew_tolerance_s", 0.05) + 1e-9, skew
    depth_m = data["depth_raw"][0].astype(np.float32) * calibration["depth_scale_meters"]
    valid = depth_m[depth_m > 0]
    print("frames:", n)
    print("synthetic:", bool(data.attrs.get("synthetic", False)))
    print("RGB shape:", data["rgb"].shape)
    print("maximum image skew (s):", skew)
    print("first-frame valid depth fraction:", valid.size / depth_m.size)
    print("first-frame valid depth range (m):",
          (float(valid.min()), float(valid.max())) if valid.size else None)
    print("Parquet/RGB-D structural checks passed")
PY
```

真机数据应显示 `synthetic: False`。此检查验证文件结构、单位所依赖的字段和时间对应；视频是否清晰、动作是否正确、深度范围是否符合实际场景，仍需人工检查。

不要一次将长 episode 的全部 RGB-D 读入内存。上例对深度只读取第 0 帧，适合用于基本检查。

### 8.3 人工检查内容

- RGB 视频中物体、夹爪和目标位置是否可见，是否有冻结、明显模糊或遮挡。
- 动作与任务描述是否一致，任务是否真正完成。
- 关节单位是否为弧度，夹爪宽度是否落在合理的米制范围内。
- 目标与反馈是否有异常跳变、长期大偏差或完全不变化。
- 深度是否存在有效像素，距离是否与场景量级一致；透明、反光、过近表面可能产生空洞。
- `image_skew_s` 是否满足配置要求，源帧复用或跳帧是否异常集中。

<a id="s9"></a>
## 9. 深度图与点云导出

### 9.1 导出单帧

确认 episode RGB-D 文件已经存在后：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/export_pointcloud.sh \
  work_dirs/collection/single_piper_d435/pick_place/raw/rgbd/chunk-000/episode_000000.h5 \
  --frame 0 \
  --output work_dirs/pointcloud_episode0_frame0 \
  --denoise
```

`--frame` 从 0 开始，必须小于 episode 帧数。`--output` 目录必须尚不存在；重复导出请使用新目录名。该脚本读取已有文件，使用 RealSense/Open3D 环境，不连接相机。

输出文件：

```text
work_dirs/pointcloud_episode0_frame0/
├── color.png
├── depth_raw.png
├── depth_aligned_to_color.png
├── points.ply
└── calibration.json
```

两张深度 PNG 是 uint16 无损深度，不是彩色伪深度图。普通图片浏览器显示很暗并不能说明数据无效；读取时需要保留 16 位，再乘深度比例。`color.png` 对应 HDF5 原始 RGB，MP4 经视频编码后不保证与该图逐像素相同。

### 9.2 调整点云参数

| 参数 | 默认值 | 含义 |
|---|---|---|
| `--stride` | `2` | 每隔多少像素取样；1 更密集，也更慢 |
| `--max-depth` | `2.0` | 保留的最大深度，单位 m |
| `--voxel` | `0.005` | 体素下采样大小，单位 m；0 禁用体素下采样 |
| `--denoise` | 不启用 | 添加后进行统计离群点过滤 |

例如导出更密集的近距离点云：

```bash
bash scripts/export_pointcloud.sh \
  work_dirs/collection/single_piper_d435/pick_place/raw/rgbd/chunk-000/episode_000000.h5 \
  --frame 0 --output work_dirs/pointcloud_episode0_dense \
  --stride 1 --max-depth 1.5 --voxel 0.002 --denoise
```

### 9.3 坐标系与标定边界

点云以 `depth_raw` 为几何来源，经 RealSense 去投影得到三维点，再通过深度到彩色外参和彩色投影取颜色。输出仍在原生深度相机光学坐标系：X 向右、Y 向下、Z 向前，单位 m。

D435 的内参和深度到彩色外参，不等于相机到机械臂基座的外参。要得到机械臂基座坐标系中的点，必须另外标定真实变换：

```text
p_base = R_base_from_depth @ p_depth + t_base_from_depth
```

固定相机需要相机到基座标定；如果相机装在末端，则需要手眼标定以及与图像时刻匹配的机器人位姿。当前代码没有自动完成这些标定，不会自动把 PLY 转为基座坐标。

手动处理保存的 RealSense 外参时，`rotation_column_major` 是列优先排列，NumPy 应使用 `reshape(3, 3, order="F")`。优先使用 RealSense 的变换 API，避免行列顺序错误。

<a id="s10"></a>
## 10. 多任务、会话管理与参数调整

### 10.1 添加任务

编辑配置的 `tasks` 部分，例如：

```json
{
  "pick_place": [
    ["pick up the red block and place it in the tray", 50],
    ["pick up the blue block and place it in the tray", 50]
  ],
  "stack_blocks": [
    ["stack one block on top of another", 30]
  ]
}
```

这只是 `tasks` 字段的值，需要放入完整配置。外层名称用于数据集选择和目录组织，列表内的文本用于任务描述。建议名称简短、稳定，不在名称中使用路径分隔符。

停止采集后修改配置，再执行 `check` 和启动。不要依赖运行中的程序自动读取 JSON 修改。

### 10.2 分开管理不同采集会话

建议给正式会话使用单独配置和输出目录，例如 `work_dirs/collection/session_20260916_01`。同一会话可继续录多段，由 EVA 管理 episode 编号；更换相机位置、工具、夹爪行程或主要采集参数时，应保留新的配置和标定记录。

不要并行运行两个采集进程写入同一输出目录。也不要手工把多个数据集的 episode 文件混到同一个目录，忽略元数据和编号冲突。

### 10.3 调参顺序

先使用默认 640 × 480、30 FPS 和低速参数完成验收。操作幅度过大时先调整 `robot.scale`，关节变化过快时结合 `robot.max_dq` 与 SDK 速度检查。所有调整都应停止录制、修改配置、重新检查，再做短段试采。

如果磁盘或处理速度跟不上，先检查设备、保存队列和 CPU/磁盘负载，再考虑降低到设备实际支持的帧率或分辨率。不要只提高同步容差来掩盖严重的数据延迟。

改变 `robot.hand` 只改变手柄来源，当前 EVA 数据仍以单臂 `right_arm` schema 存储。需要双臂时必须另外修改机器人和数据 schema，不能仅靠改手柄字段实现。

<a id="s11"></a>
## 11. 补关联、备份与训练交接

### 11.1 保存后缺少 RGB-D 文件

先等 EVA 的视频和 Parquet 转换完成，再运行：

```bash
cd ~/picoxpiper/PiperXR-master
bash scripts/collect.sh link
```

自定义配置：

```bash
bash scripts/collect.sh link --settings "$PWD/configs/collection_session_01.json"
```

模拟目录：

```bash
bash scripts/collect.sh link --mock-data
```

补关联只处理已有数据，不启动相机或机械臂。它根据 Parquet、原始 RGB-D 归档及 EVA 消费帧清单重建 episode 配套 HDF5；已有最终文件会被跳过，命令不会主动校验或修复一个已经存在但损坏的最终文件。

补关联不能从缺失的原始帧中恢复深度，也不能代替尚未完成的 EVA 视频/Parquet 转换。执行无输出时，应检查是否存在可关联的 Parquet、完整归档以及正确输出目录。

### 11.2 中断文件与原始归档

写入未完成可能留下 `*.partial.h5`。不要把它改名后当作完整 episode，应该保留现场并检查日志和源文件。属性 `complete` 也不能代替对文件结构和所有输出文件的完整性检查。

原始归档不会自动清理，取消的片段也可能保留。至少在正式 episode 已通过结构检查、人工质检并完成备份之前，应保留 `raw_rgbd`。清理后会失去部分重建和排错能力。

### 11.3 备份范围

建议在停止采集并确认保存完成后，备份整个会话输出目录和当次使用的 JSON 配置。保留文件相对目录、episode 编号、标定信息、任务文本和质检记录。

如果目标磁盘已经挂载、且安装了 `rsync`，可参考下面命令。将目标路径替换为实际备份位置后执行：

```bash
rsync -a --info=progress2 \
  work_dirs/collection/single_piper_d435/ \
  /mnt/backup/piper_session_20260916_01/
```

不要直接假设 `/mnt/backup` 是本机已挂载的磁盘。复制结束后检查目标盘内容和容量，至少抽查一个 episode 的 Parquet、视频和 RGB-D。

### 11.4 给训练流程的数据

标准 EVA/LeRobot 消费端使用 `data/`、`videos/` 和 `meta/`。需要深度或点云时，同时携带 `rgbd/`，并在训练数据加载器中按 episode 与 `frame_index` 读取配套深度；当前集成不会自动为任意训练框架新增深度特征。

**EVA 的普通 QUALITY EXPORT 不会自动复制自定义 `rgbd/` 目录。** 如果该导出筛选或重新编号 episode，也不能直接按导出后的同名编号拼接原始深度；需要保存并使用源 episode 的对应关系。最直接的完整交接方式是保留原始 `<数据集>/raw/` 目录及所需的人工筛选记录。

<a id="s12"></a>
## 12. 常见故障与处理顺序

### 12.1 启动失败时先保留根因

启动器会在终端打印本次 `Bridge/camera/robot log:` 路径，并把 bridge 及两个 worker 的标准输出和错误输出保存在会话目录的 `logs/bridge_*.log`。原始输出也会继续显示在终端。启动失败时，最终异常包含子进程退出码、日志路径和最后 100 行以内的输出摘要（最多读取末尾 16 KiB）。

`Collection bridge failed during startup` 是上层汇总错误。需要查看它之前的第一个 worker 异常；`Connection reset by peer` 经常只是 worker 先退出导致的后续现象。日志保存于实际配置的输出目录，mock 模式则位于对应 `_mock` 目录。

例如查看默认真机目录中最近修改的日志文件名：

```bash
ls -lt work_dirs/collection/single_piper_d435/logs/
```

根据启动器打印的完整路径打开对应文件。终端摘要未覆盖根因时，以完整日志为准。

### 12.2 已修正的 PiPER SDK 初始化问题

真机曾报告：`Interface 'can_auto_init' is False and '__arm_can' is None!!`。原因是采集适配器设置 `can_auto_init=False` 后，直接调用了 `ConnectPort()`，缺少 SDK 要求的 `CreateCanBus()`。

现已在 `robot_worker.py` 改为：创建 SDK 对象 → `CreateCanBus(can_name=..., bustype="socketcan", expected_bitrate=1000000, judge_flag=True)` → `ConnectPort()`。它检查并打开已配置的 CAN 接口，不运行 `ip link` 配置命令，也不在连接阶段使能电机。

修复已用本机真实 SDK 加模拟底层 CAN I/O 验证；真机反馈、运动和整段录制仍需重试验收。如果后续报接口不存在、未 UP 或波特率不符，再按第 5.3 节检查实际接口。

### 12.3 CAN 已连接但关节或夹爪反馈缺失

如果启动报 `PiPER ... CAN feedback is missing or stale`，它表示启动等待结束后仍未得到足够新的关节和夹爪反馈，不等同于 PICO 或 D435 失败。退出时的 `CANCELLED`、`uninitialize sdk` 和后续 IPC 连接重置，可能是机器人 worker 退出后的清理输出。

当前代码会分别报告 `joints` 和 `gripper` 的 SDK 时间戳、相对当前时间的 `age`、SDK Hz 以及状态：

| 状态 | 含义 |
|---|---|
| `missing` | SDK 时间戳为 0，尚未接收到或成功解析该类反馈 |
| `fresh` | 时间戳有效且距离当前时间小于 0.3 秒 |
| `stale` | 收到过反馈，但已超过 0.3 秒 |
| `future_timestamp` | 帧时间戳晚于采样的主机时间，需要检查实际时间戳与系统时钟 |
| `invalid_timestamp` | 时间戳为非有限数值 |

本机 SDK 将 Python CAN 帧中的 `timestamp` 直接放入反馈消息；SocketCAN 使用 Unix 秒时间戳。不要在没有证据时除以 1000、自动替换为当前时刻，或取消新鲜度检查。SDK 返回可被收包线程修改的对象，代码现在先复制时间戳，再读取比较用的主机时间，避免并发更新引入“未来时间”误判。

先停止采集进程和其他主动控制程序，再执行独立诊断：

```bash
bash ~/picoxpiper/PiperXR-master/scripts/diagnose_collection_can.sh
```

上面是一整行命令，可从任意目录执行。启动脚本自动使用本机 `pico_teleop` Python，默认检查 `can0`、持续 5 秒，并将结果写入项目的 `work_dirs/collection/can_diagnostic.json`；父目录会自动创建。若接口不是 `can0`，在同一行末尾添加 `--can 实际接口名`。不要把 `.py` 文件名或报告路径单独作为命令执行。

该脚本只打开 SocketCAN 接收端、用 SDK 解码收到的帧；不调用 SDK `ConnectPort`、参数查询、使能、运动指令或 `ip link set`，不连接 PICO 和 D435。默认 5 秒退出，最多允许 60 秒。它打印诊断前后的接口状态、各标准数据帧 ID 的计数、最后时间戳、缺失反馈 ID 和解析结果，并保存 JSON 报告。

判读顺序：

1. 报告存在 `error` 时，先解决接口打开或权限问题，不能将“未能打开”理解为“打开后没有帧”。
2. `standard_data_frames = 0` 时，检查是否选中了正确 CAN 口、机械臂供电、CAN 接线、1 Mbps 和总线错误状态。旧日志中的 `SEND_MESSAGE_FAILED` 也应结合错误计数分析。
3. 应持续收到 `0x2A5`（关节 1/2）、`0x2A6`（关节 3/4）、`0x2A7`（关节 5/6）和 `0x2A8`（夹爪）。`missing_expected_ids` 可定位缺失部分。
4. 关节反馈齐全但仅缺 `0x2A8` 时，再检查实际夹爪接线和启用的末端类型/参数。SDK 提供夹爪与示教器参数配置功能，但需要根据真实硬件决定，不能直接照抄默认配置或伪造夹爪反馈。
5. 有其他帧、却缺目标 ID 时，检查机械臂模式、反馈 ID 是否有偏移、固件与协议是否匹配；保留实际 ID 列表，不直接改控制模式尝试。
6. 所有 ID 都存在但 `stale` / `future_timestamp` 时，检查帧到达时刻、系统时钟和持续更新情况；诊断结束瞬间的 `feedback_ready=True` 也只代表短时接收通过，不代表长时间采集已经验收。

诊断器逐个检查三个关节帧 ID；SDK 的 `GetArmJointMsgs()` 只有聚合时间戳，因此只看到聚合时间戳新鲜不能单独证明三个关节组都持续更新。被动诊断不启动 SDK 的频率计算线程，输出中的 `SDK_Hz` 可能为 0；以接收帧数与时间戳为准。

#### 早期被动诊断：接口正常打开，但 5 秒内无新数据

以下为早期零帧记录。后续固件查询已成功，最新结论及处理步骤见第 12.4 节末尾；对于已收到固件应答的情况，无需重复本段供电/接线基础排查。

2026-09-16 的本地诊断报告 `work_dirs/collection/can_diagnostic.json` 显示：

| 项目 | 观测结果 | 可以得出的结论 |
|---|---|---|
| 接口 | `can0`，`UP`，`bitrate 1000000` | Linux 接口已启动，配置波特率符合当前采集要求 |
| 当前 CAN 状态 | `ERROR-ACTIVE` | CAN 正常可通信状态名称，本身不表示当前正在报错；也不证明机械臂在线 |
| 新接收标准数据帧 | `0` | 本次 5 秒窗口未收到标准数据帧 |
| 接口累计 RX packets | 检查前后均为 `6717634` | 该窗口接口计数没有增长；累计数量不是当前仍有反馈的证据 |
| 关节/夹爪时间戳 | 都为 `0` | 解码器没有获得有效关节和夹爪反馈，不是已有反馈超过容差 |
| `error-pass` | 检查前后均为 `266808` | 历史状态变化计数较高；本次未增长，不能直接判定当前为 ERROR-PASSIVE |
| USB-CAN 标识 | `gs_usb`，`parentdev 3-7:1.0` | 用于核对 `can0` 是否对应实际接线的适配器，重新插拔后路径可能变化 |

此时先确认机械臂本体供电与控制器指示灯。USB-CAN 被电脑识别并不证明机械臂本体已上电。随后按设备接线说明检查适配器到机械臂的通信连接、CAN_H/CAN_L 和终端配置；需要接插或重新接线时按机械臂规程操作。若有多个适配器，运行 `ip -details link show type can` 核对端口，不能默认 `can0` 永远对应同一台设备。

如果已经确认本体供电、接线和接口正确，并已停止采集及其他控制程序，可在本地终端依次重启 CAN 接口，然后重新运行被动诊断。以下操作会短暂断开 CAN，但不发送机械臂运动或使能指令：

```bash
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up
bash ~/picoxpiper/PiperXR-master/scripts/diagnose_collection_can.sh
```

若其他 CAN 接口才是实际设备，应先更正接口选择。诊断出现四个预期反馈 ID 且 `feedback_ready` 为 `true` 后，再回到真机采集入口；如果仍无新 RX，应进一步核对机械臂当前模式、是否曾用相同接线和接口成功控制、供电/连接和适配器状态。不能仅凭这份零帧报告确定某个硬件已损坏，也不能直接假定必须发送模式切换或夹爪配置指令。

### 12.4 同一接线曾在 PiperXR 正常使用，但当前被动接收仍为零

本机用户确认曾通过原 PiperXR、同一 `can0` 成功控制。代码对比发现：原 `PiperArmProxy.connect()` 执行 SDK 连接后，立即 `EnableArm(7)` 并设置 CAN 关节控制模式；数采入口先等待新鲜的关节/夹爪反馈，再由 MOTION 和有效 PICO grip 授权使能。两条路径的 SDK `ConnectPort()` 都包含 `PiperInit()` 的参数/固件查询，因此不能简单归因为数采入口没有执行 SDK 查询。

这个启动顺序差异需要验证。两次被动零帧结果本身既不能证明必须先使能才有反馈，也不能证明当前硬件损坏。先执行一次固件查询，以检查控制器是否仍能通过 CAN 应答：

```bash
bash ~/picoxpiper/PiperXR-master/scripts/diagnose_collection_can.sh --query-firmware
```

该模式与默认的完全被动接收不同：它在 5 秒内最多发送 3 条固件读取请求，报文与本机 SDK 的 `SearchPiperFirmwareVersion()` 一致，CAN ID 为 `0x4AF`，数据为 `01 00 00 00 00 00 00 00`。它不调用电机使能、运动模式切换、关节/夹爪目标、主从配置或参数写入功能。运行前停止其他主动控制程序，以便判断回复来源。

**若显示 `DOWN / STOPPED` 或 `Network is down [Error Code 100]`，先恢复接口再查询。** 本机一次报告中接口编号由 9 变为 10，所有计数清零且未显示 bitrate，符合接口重新创建后的状态，可能由 USB 重新插拔触发。接口名称仍叫 `can0` 不代表原波特率和 UP 状态被保留。这个新状态可以解释该次查询发送失败，不能用它反推之前 UP 时零反馈的原因。

停止采集和其他主动控制程序，逐行执行，前一条成功后再执行下一条：

```bash
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up
ip -details link show can0
```

确认接口标志包含 `UP`、显示 `bitrate 1000000`，且 CAN 不再是 `STOPPED`，再运行本节的 `--query-firmware` 命令。系统重启、USB 适配器重新插拔后应重新检查接口状态。这些命令配置电脑的 CAN 接口，不调用机械臂电机使能。

诊断器现在会先检查接口：DOWN/STOPPED、非 1 Mbps、BUS-OFF 或无法读取接口状态时，保存 `error` 并结束，`diagnostic_started=false`，不再连续发送注定失败的请求。接口状态检查与实际采样之间仍可能发生拔线等变化，后续发送错误仍会记录到报告中。

结果默认单独保存为 `work_dirs/collection/can_firmware_diagnostic.json`，保留已有被动诊断报告。新增字段的含义：

| 字段 | 含义 |
|---|---|
| `mode = firmware_query`、`passive = false` | 本次包含固件读取请求，不是完全被动监听 |
| `firmware_queries` | 每次请求的发送结果；`queued = true` 仅表示进入本机发送队列，不证明已送达或已得到 CAN ACK |
| `diagnostic_started` | 是否通过接口检查并开始采样；为 false 时先解决 `error` 中的接口问题 |
| `frames["0x4AF"]` | 观察到的接收帧数与示例数据；本机发送回显被排除 |
| `firmware_version` | SDK 解码出的固件版本字符串；`null` 表示没有成功解析到版本，不能直接据此断定硬件故障 |
| `error_examples` | 接收到的 CAN 错误帧示例，便于与接口计数一起分析 |
| `feedback_ready` | 关节/夹爪反馈是否齐全且新鲜；即使固件应答正常，该值仍可能为 false |

若收到固件版本但没有周期关节反馈，下一步检查机械臂模式、反馈配置及启动顺序；若发送报缓冲区不足或在窗口内没有任何回复，则继续核对适配器和总线传输状态。不要把“请求已排队”等同于通信恢复。此功能已通过真实 SDK 报文核对和模拟应答/无应答/发送失败验证，实际结果仍以本地报告为准。

#### 2026-09-16 真机结果：收到 S-V1.8-8，周期状态仍缺失

2026-09-16，用户恢复 `can0` 为 UP / 1 Mbps 后，`work_dirs/collection/can_firmware_diagnostic.json` 记录了以下实际结果：

| 项目 | 结果与含义 |
|---|---|
| 固件查询 | 3 条 `0x4AF` 请求，实际收到 33 条 `0x4AF` 回复；固件为 `S-V1.8-8` |
| 接口计数 | RX 从 0 增至 33、TX 从 0 增至 3，本次错误计数未增加 |
| 周期数据 | 接收 ID 只有 `0x4AF`，没有 `0x2A5`～`0x2A8`，也未观察到偏移后的其他状态 ID |
| 数采状态 | 关节与夹爪时间戳仍为 0，`feedback_ready=false`，启动等待仍会失败 |

这次已证明电脑与控制器之间的查询收发可用。还不能确定周期数据缺失的具体原因，也不能凭固件应答证明电机及内部反馈链路正常。相机配置和时间戳容差不能解决“没有收到状态帧”。

本机 SDK 的 `demo/V2/piper_read_joint_state.py` 只调用 `ConnectPort()` 后读取状态，并不先调用 `EnableArm()`。原 PiperXR 启动时会使能并切换关节控制模式，但目前没有证据证明本例必须先使能才能读到反馈。保留数采入口的反馈有效性检查。

下一步进行一次控制器冷启动对比：

1. 停止数采、原 PiperXR、SDK 示例及其他 CAN 控制程序。按设备规程使机械臂停稳并妥善支撑，避免断电后下落。
2. 关闭 **PiPER 本体电源**，待控制器指示灯熄灭后约 10 秒再上电，等待启动完成。此前 `ip link set can0 down/up` 或重新插拔 USB-CAN 只改变电脑侧接口，不能替代本体重启。这一步用于检查暂时性控制器状态，不保证恢复周期上报。
3. 保持其他控制程序关闭，执行下方单行命令，持续监听 10 秒；它只接收，不查询、不使能。若提示接口 DOWN，先按本节已有三条 `ip link` 命令恢复 UP / 1 Mbps，再重试。

```bash
bash ~/picoxpiper/PiperXR-master/scripts/diagnose_collection_can.sh --seconds 10
```

被动报告写入 `work_dirs/collection/can_diagnostic.json`，已有固件查询报告不会被这条命令覆盖。

- 四个预期 ID 持续出现且 `feedback_ready=true`：结束诊断后回到第 6 节真机入口，先做短段采集验收。
- 仍为零帧：控制器冷启动也没有恢复周期状态；结合已成功的固件查询，继续核实 S-V1.8-8 的控制模式、周期上报设置与内部反馈状态。不能将它解释为 CAN 收发完全不通。
- 只有部分反馈、其他 ID 或异常时间戳：保留完整 `frames`、`streams`、`missing_expected_ids`，据实定位；不要填零或跳过检查。

本机 SDK 只为 `0x422` 声明了“CAN 升级总线静默模式”枚举，没有提供可核实的发送方法或载荷，不能据此断言当前处于静默模式或猜测恢复字节。另外，`ArmParamEnquiryAndConfig(...data_feedback_0x48x=...)` 描述的是 `0x481`～`0x486` 速度/加速度上报，不是本次缺失的 `0x2A5`～`0x2A8`。若冷启动后仍缺状态，需要结合适配 S-V1.8-8 的官方协议或厂商说明确定下一条配置指令。

#### 2026-09-17：本体发送控制目标，单次恢复运动输出角色

本体重启后的 10 秒被动诊断收到 29 帧：`0x151`、`0x155`～`0x157`、`0x159`、`0x190`、`0x471`，仍无 `0x2A5`～`0x2A8`。RX 从 143 增至 172，TX 保持 0，诊断器也排除了本机发送回显。用户确认 USB-CAN 直连 PiPER 本体，没有示教器或其他控制器，插座未看到文字标识。

本机 SDK 将 `0x155`～`0x157`、`0x159` 定义为主臂发送给从臂的关节/夹爪目标；`0x151` 示例指令包含速度 100% 和 MIT 标志 `0xAD`。这些是指令内容，不能代替实测反馈，也不能用它们断言指令已执行。`0x190` 在本机 SDK 未找到定义。当前优先怀疑本体处于主臂/示教输入角色；缺少状态帧及接口标识，仍不能确认根因。

对于本会话已确认的单臂直连用途，可进行一次 **恢复运动输出角色及默认 CAN ID** 的配置尝试。依据是本机 SDK 的 `demo/V2/piper_set_slave.py`：调用 `MasterSlaveConfig(0xFC, 0, 0, 0)`，从主臂切换后需要重新启动机械臂。“从臂/运动输出臂”表示接收电脑控制指令的设备角色，单臂使用也适用，并不要求另接一台主臂。

独立脚本 `scripts/restore_collection_can.sh` 默认仅预览；只有传入 `--apply` 才发送以下一条配置帧：

```text
CAN ID: 0x470
Data:   FC 00 00 00 00 00 00 00
```

它将角色设为运动输出，并把反馈、控制、联动目标三个 ID 偏移恢复为 0。它是 **设备配置写入**，不是被动诊断；角色切换可能改变控制器工作状态，不能因为没有位置目标就保证机械臂的保持状态不变。脚本不发送电机使能/失能、关节/夹爪目标、回零或出厂复位指令，也不会被数采启动器自动调用。

操作顺序：

1. 停止数采、原 PiperXR 和其他控制程序，按设备规程让机械臂停稳、妥善支撑，保持本体上电以接收配置。
2. 确认 `can0` 已为 UP / 1 Mbps，执行下面一整行。脚本会先检查接口，仅发送一次、不自动重试，并保存独立报告。

```bash
bash ~/picoxpiper/PiperXR-master/scripts/restore_collection_can.sh --apply
```

报告保存到项目 `work_dirs/collection/can_role_restore_<时间>_<唯一标识>.json`。`request.queued=true` 仅表示发送调用成功入队，**不代表设备已接受配置**；`configuration_verified` 仍为 false。返回码 0 同样不代表反馈已经恢复。若报错，先查看 `error`；接口 DOWN 时按本节前面的 CAN 配置步骤恢复后再操作。指定 `--output` 的文件若已存在，脚本在访问 CAN 前退出，不覆盖已有报告。

3. 发送未报错后，待脚本退出，按设备规程关闭 PiPER 本体电源；指示灯熄灭约 10 秒后重新上电，等待启动完成。这次重启是使角色配置生效的步骤，与此前单纯重启排查不同。
4. 保持其他控制程序关闭，执行下面一整行，验收实际反馈。若 USB-CAN 接口重新创建而变为 DOWN，先恢复 UP / 1 Mbps。

```bash
bash ~/picoxpiper/PiperXR-master/scripts/diagnose_collection_can.sh --seconds 10
```

验收要求是四个反馈 ID 均出现、`joints` 与 `gripper` 都为 `fresh`、`feedback_ready=true`。通过后再进入第 6 节真机采集，先做短段验收。若仍只有 `0x15*` 控制目标、只有部分反馈，或完全无数据，应保留恢复配置报告和新的被动报告，再核实接口用途、配置是否被固件接受及设备角色；不要循环执行配置或把控制目标当作状态反馈。

脚本已使用真实 SDK 编码和模拟 CAN 验证，未由助手向真机发送配置，恢复效果仍待用户本地操作确认。

### 12.5 故障对照表

| 现象或报错 | 排查与处理 |
|---|---|
| `check` 缺少模块或解释器不存在 | 核对三个环境路径和出错解释器；按对应项目修复缺失依赖，再跑 check |
| 提示机器人/transport 类型未注册 | 确认从 `bash scripts/collect.sh ...` 启动，不直接用普通 EVA 入口加载生成配置 |
| 页面打不开、`Address already in use` | 看采集终端是否启动成功，用 `ss -ltnp` 检查端口；在对应终端结束旧实例，或在配置中调整冲突端口 |
| `Collection sockets must bind to localhost` | 三个 ZMQ endpoint 改回 `tcp://127.0.0.1:端口`；PICO 连接配置独立处理 |
| `Camera/robot startup timed out` 或 `Hardware worker stopped` | 查看该错误之前的 camera/robot worker 日志，再分别做 D435 和 CAN 检查 |
| D435 未找到、忙碌或流启动失败 | 检查 USB3 接线、占用相机的 Viewer/脚本、序列号与支持的流组合；先通过独立相机 check |
| `could not initialize udev monitor` | 检查运行环境是否限制 USB/udev；在有实际设备访问权限的本地终端复查，不能仅据此判定相机未连接 |
| `PiPER ... CAN feedback is missing or stale` | 按第 12.3 节运行只读 CAN 诊断，区分关节缺失、夹爪缺失、过期或时间戳异常 |
| `can_auto_init ... False` / `__arm_can ... None` | 确认运行的是已包含 `CreateCanBus()` 修复的本项目采集入口；该错误不等同于相机故障 |
| `Invalid PiPER feedback or gripper stroke configuration` | 核对夹爪反馈和真实行程，调整 `gripper_open_m`，不要用放大行程掩盖异常反馈 |
| `PICO input is missing or stale` | 检查 PC 服务、头显连接、手柄追踪及使用的是右手还是左手 |
| `Motion is locked; switch EVA MOTION off and on to re-arm` | 心跳或数据源异常使授权失效；排除原因、释放 grip，再手动关闭并重新开启 MOTION |
| 开启 MOTION 后机械臂不动 | 检查 PICO 数据、grip 是否按到位和实际使能反馈；连接/授权本身并不等于已开始运动 |
| `START RECORD` 不可点击 | 检查数据集和任务是否选中、MOTION 是否开启、保存队列是否已满 |
| HOME、手动关节或策略控制报错 | 当前入口仅支持 PICO 数采；用 PICO 在录制前调整起始位 |
| `Robot/camera sample skew ... exceeds limit` | 检查相机帧率、机器人反馈是否停滞、CPU 和磁盘负载；恢复后先短段验收 |
| `RGB-D disk writer cannot keep up` | 检查磁盘剩余空间和写入速度，减少同时运行的重任务，按硬件支持情况降低采集负载 |
| `RGB-D skew exceeds ...` | 检查源图像到 EVA 保存网格的时间误差与帧丢失；保留源归档和日志，不强行错配 |
| Parquet/视频存在，但没有 RGB-D | 等保存结束后运行对应输出配置的 `link`，检查完整源归档是否还在 |
| 取消段仍有 HDF5 | 原始 RGB-D 暂存保留是当前设计；以正式 `<任务>/raw/data/` 中的 episode 为准 |
| 点云导出 `FileExistsError` | 使用一个尚不存在的输出目录 |
| 点云帧索引越界 | 使用 `0 <= frame < N`，先按第 8 节查看帧数 |
| `No valid points or PLY writing failed` | 检查深度是否有效、距离阈值是否过小、文件写入权限及 RGB/深度标定 |
| 点云看起来方向不对 | 当前输出是深度光学系，先确认坐标约定，再检查相机到基座外参，不直接反转坐标凑效果 |

出现机械动作异常时，先按现场硬件停止规程处理；数据故障排查应在机械臂处于可控状态后进行。对于纯文件问题，优先保留源文件和日志再做恢复。

<a id="s13"></a>
## 13. 验证命令和已知边界

### 13.1 可重复的软件验证

以下命令从项目根目录执行。测试输出目录必须尚不存在；再次运行请换新的目录名。测试使用模拟数据，不应同时占用正式采集输出目录。

```bash
cd ~/picoxpiper/PiperXR-master
~/EVA-CLIENT/.venv/bin/python -m pytest -q tests/collection
~/anaconda3/envs/pico_teleop/bin/python scripts/validate_collection_robot.py
~/EVA-CLIENT/.venv/bin/python scripts/validate_collection_offline.py \
  --output /tmp/piperxr_offline_check_new
~/EVA-CLIENT/.venv/bin/python scripts/test_collection_network.py \
  --output /tmp/piperxr_network_check_new
```

`validate_collection_robot.py` 先使用本机安装的真实 PiPER SDK、模拟底层 CAN I/O 和 XR 输入验证初始化顺序，再使用 mock SDK 验证单位、限幅和反馈检查。它不连接真实硬件。`validate_collection_offline.py` 调用真实 EVA 离线录制和转换流程，但输入是合成数据。

`test_collection_network.py` 强制使用 mock，使用独立的本机端口 `15555`–`15557`、`15757`、`18080`，执行有超时限制的联调并清理其启动的服务，不连接真实硬件。

### 13.2 截至本手册编写时的结果

| 验证项 | 状态与证据 |
|---|---|
| 三个环境的导入和 EVA 配置解析 | 已通过 |
| 数采及启动诊断测试 | 15 项通过，覆盖原数据/控制逻辑、子进程原始错误保留，以及关节/夹爪新鲜度、错误分类和 SDK 共享对象时间戳读取 |
| 本机真实 SDK 初始化逻辑 | 修复前用模拟 CAN I/O 复现 `__arm_can is None`，修复后 `CreateCanBus → ConnectPort → DisconnectPort` 通过；连接阶段未调用使能/运动指令 |
| mock CAN 控制路径 | 已通过，包含夹爪字段、单位、使能码、限幅后的发送目标与陈旧反馈拒绝 |
| 项目原有控制相关测试 | 11 个测试函数在 Piper 环境中直接执行通过；该环境没有 pytest |
| EVA 离线“保存、取消、保存” | 已通过；最终两段分别为 36、35 帧，自动质量为 green，视频/Parquet/深度帧数一致 |
| RealSense/Open3D 文件导出 | 已通过合成帧验证，输出 RGB、16 位深度 PNG 和包含 719 个着色点的 PLY |
| 初始 mock 服务启动 | 已到达 EVA 页面及单臂模型显示；此前启动的 mock 服务已结束 |
| 完整本机网络“保存、取消、保存” | 尚未完成；此前自动审批服务持续 HTTP 503 阻止最后测试，即使用户已授权。可在本地运行上述 mock 网络测试 |
| 真实 D435 + CAN + PICO 联采 | CAN 初始化及固件查询已通过；本体冷启动后收到控制目标而没有状态反馈，用户确认单臂直连，按第 12.4 节准备了运动输出角色恢复脚本；配置生效、状态恢复及整段联合录制尚未实测通过 |
| 夹爪真实行程与长期写入性能 | 尚未实测 |
| 相机到机械臂基座/末端外参 | 尚未标定 |

离线验证产物曾保存到 `/tmp/piperxr_collection_verified/`，点云产物位于 `/tmp/piperxr_pointcloud_verified/`；这些只是临时验收文件，重启或清理后可能消失，不能当作长期备份。

已有 EVA/imageio/JAX 视频编码路径曾输出 `os.fork()` 与 JAX 多线程相关警告，已测短段转换完成。长时间采集仍需要独立验证，不能据此保证没有性能或资源问题。

详细记录见 [docs/collection_validation.md](docs/collection_validation.md)。文档与修复验证使用代码检查、模拟 I/O 和已有模拟文件；用户提供的设备识别日志不代表联合采集完成，也未新增完成表中未验证的硬件或网络测试。

<a id="s14"></a>
## 14. 一次采集的验收清单

### 开始前

- [ ] 已确认本次 JSON、任务和输出目录。
- [ ] `collect.sh check` 通过。
- [ ] D435 独立检查通过，USB3 连接，其他相机程序已退出。
- [ ] PiPER CAN 接口和 1 Mbps 正确，没有其他控制程序。
- [ ] PICO 已连接 PC 服务，手柄追踪和左右手选择正确。
- [ ] 机械臂、相机固定，工作区域和硬件停止方式已确认。
- [ ] 磁盘空间足够，首次或变更配置后已做 10–20 秒试采。

### 每段结束后

- [ ] 使用 `END / SAVE` 保存，或明确 `CANCEL` 放弃。
- [ ] 保存队列完成，无未处理的报错。
- [ ] 任务描述、视频内容和机械臂动作一致。
- [ ] 人工质检与失败原因已记录。
- [ ] 对应 Parquet、视频、RGB-D 和标定信息齐全。

### 整场结束后

- [ ] 已释放 grip、结束当前段、关闭 MOTION。
- [ ] 采集进程正常退出，按机械臂规程完成硬件收尾。
- [ ] 必要时运行 `link`，抽查状态/动作和深度关联。
- [ ] 完整会话目录及使用的配置已备份。
- [ ] 深度/点云训练交接包含 `rgbd/` 和标定，未误用仅包含视频的普通导出。
- [ ] 在核验与备份完成前保留原始 `raw_rgbd` 归档。
